function setBoardColor(color)
{
	alert(color);
	var tblChessBoard = document.getElementById('tblChessBoard');
	//for (var rows = 0; cols < tblChessBoard.rows.lenght; rows++)
	//{
		//for (var cols = 0; cols < rows.cells.length; cols++)
			//alert(tblChessBoard.rows[rows].cells[cols].innerText);
	//}
}